# Invented provider-response contract fixtures

These sanitized Slice 3A fixtures model only the candidate file-transcription response boundary.
They are invented, synthetic, and contain no production request or response, request identifier,
organization identifier, credential, recording, client content, or copied written fixture.

They are exercised only through an injected, network-blocked SDK transport. They are not evidence
of live model quality, model availability, account approval, or data-control configuration.
